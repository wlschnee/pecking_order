$("div[location_id='<%= @location.id %>'] span").attr('class', "<%= @location.thumbs_up_total(current_user) %>")
$("div[picture_message_id='<%=@location.id%>']").html("<%= @location.likes_message(current_user) %>")
